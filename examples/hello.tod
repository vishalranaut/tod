let name = "TOD";
fn greet(user) {
  return "hello " + user;
}
if (name == "TOD") {
  log(greet("world"));
}

let n = 3;
while (n > 0) {
  log("n = " + n);
  n = n - 1;
}

fn fib(x) {
  if (x <= 1) {
    return x;
  }
  return fib(x - 1) + fib(x - 2);
}
log("fib(10) = " + fib(10));
